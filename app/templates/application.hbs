<div class="container">
    <div class="row">
        <div class="col-md-10">
            Welcome to Darkstar Portal!
        </div>
        <div class="col-md-2">
            {{#if session}}
                Hello, {{session.account.login}}
            {{/if}}
            {{#unless session}}
                {{#link-to "login"}}Login{{/link-to}}
                {{#link-to "register"}}Register{{/link-to}}
            {{/unless}}
        </div>
    </div>
    <div class="row">
        <div>
            {{outlet}}
        </div>
    </div>
</div>
